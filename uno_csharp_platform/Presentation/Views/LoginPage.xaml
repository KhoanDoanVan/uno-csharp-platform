<Page x:Class="uno_csharp_platform.Presentation.Views.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:vm="using:uno_csharp_platform.Presentation.ViewModels"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel HorizontalAlignment="Center" 
                       VerticalAlignment="Center" 
                       MaxWidth="400" 
                       Padding="20">
                
                <TextBlock Text="ShopApp" 
                          FontSize="48" 
                          FontWeight="Bold" 
                          HorizontalAlignment="Center" 
                          Margin="0,0,0,40"/>

                <TextBlock Text="Đăng Nhập" 
                          FontSize="32" 
                          FontWeight="SemiBold" 
                          HorizontalAlignment="Center" 
                          Margin="0,0,0,30"/>

                <TextBox Header="Tên đăng nhập" 
                        Text="{x:Bind ViewModel.Username, Mode=TwoWay}" 
                        Margin="0,0,0,15" 
                        PlaceholderText="Nhập tên đăng nhập"/>

                <PasswordBox Header="Mật khẩu" 
                            Password="{x:Bind ViewModel.Password, Mode=TwoWay}" 
                            Margin="0,0,0,20" 
                            PlaceholderText="Nhập mật khẩu"/>

                <TextBlock Text="{x:Bind ViewModel.ErrorMessage, Mode=OneWay}" 
                          Foreground="Red" 
                          TextWrapping="Wrap" 
                          Margin="0,0,0,15" 
                          Visibility="{x:Bind ViewModel.ErrorMessage, Mode=OneWay, Converter={StaticResource EmptyToCollapsedConverter}}"/>

                <Button Content="Đăng Nhập" 
                       Command="{x:Bind ViewModel.LoginCommand}" 
                       HorizontalAlignment="Stretch" 
                       Margin="0,0,0,15" 
                       IsEnabled="{x:Bind ViewModel.IsLoading, Mode=OneWay, Converter={StaticResource InverseBoolConverter}}"
                       Style="{StaticResource AccentButtonStyle}"/>

                <ProgressRing IsActive="{x:Bind ViewModel.IsLoading, Mode=OneWay}" 
                             HorizontalAlignment="Center" 
                             Margin="0,0,0,15"/>

                <StackPanel Orientation="Horizontal" 
                           HorizontalAlignment="Center">
                    <TextBlock Text="Chưa có tài khoản? " 
                              VerticalAlignment="Center"/>
                    <HyperlinkButton Content="Đăng ký ngay" 
                                    Command="{x:Bind ViewModel.NavigateToRegisterCommand}"/>
                </StackPanel>

                <TextBlock Text="Demo: username=demo, password=123456" 
                          FontSize="12" 
                          Foreground="Gray" 
                          HorizontalAlignment="Center" 
                          Margin="0,20,0,0" 
                          TextWrapping="Wrap"/>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>